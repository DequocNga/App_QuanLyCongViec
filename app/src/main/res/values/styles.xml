<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/select_condition_button</item>
        <item name="colorPrimaryDark">@color/login_button_successfully</item>
        <item name="colorAccent">@color/bg_swipe_group_item_pinned</item>
    </style>

</resources>
